tasks:
  build:
    command: 'bun run build'
    deps:
      - 'express:build'
    inputs:
      - 'src/**/*'
      - 'tsconfig.json'
      - 'package.json'

  dev:
    command: 'bun run dev'
    deps:
      - 'express:build'
    preset: 'server'
