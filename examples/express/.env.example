# Your Authdog public key (safe to expose to the browser).
# Found in the Authdog console under your environment settings.
PK_AUTHDOG=pk_...

# Port the example server listens on (defaults to 3010).
PORT=3010
