tasks:
  build:
    command: 'bun run build'
    deps:
      - 'vue:build'
    inputs:
      - 'src/**/*'
      - 'index.html'
      - 'tsconfig.json'
      - 'vite.config.ts'
      - 'package.json'
    outputs:
      - 'dist/**'

  type-check:
    command: 'bun run type-check'
    deps:
      - 'vue:build'
    inputs:
      - 'src/**/*'
      - 'tsconfig.json'
      - 'package.json'

  dev:
    command: 'bun run dev'
    deps:
      - 'vue:build'
    preset: 'server'
