// Replace `authdogPublicKey` with your own Authdog public key (`pk_…`).
// The public key is safe to ship to the browser — it only encodes the
// identity host and environment id used to build OIDC flows.
export const environment = {
  authdogPublicKey: "pk_your_public_key_here",
};
