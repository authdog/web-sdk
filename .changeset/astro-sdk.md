---
"@authdog/astro": minor
---

Add `@authdog/astro` — Astro SDK with session middleware (`Astro.locals.authdog`), `createAuthdogServer` server helpers (`getSession`, `getUser`, `logout`), and a framework-agnostic client bootstrap (`initAuthdog`).
