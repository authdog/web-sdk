---
"@authdog/sveltekit": minor
---

Add `@authdog/sveltekit` — SvelteKit SDK with a session `handle` hook (`event.locals.authdog`), `createAuthdogServer` server helpers (`getSession`, `getUser`, `logout`), and a framework-agnostic client bootstrap (`initAuthdog`).
