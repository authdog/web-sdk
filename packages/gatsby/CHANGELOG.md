# @authdog/gatsby

## 0.1.0

### Minor Changes

- Add `@authdog/gatsby` — Gatsby SDK with a React client provider (`AuthdogProvider`), Gatsby Functions server helpers via `createAuthdog` (`getSession`, `getUser`, `requireAuth`, `logout`), and a browser token bootstrap (`initAuthdog`).
