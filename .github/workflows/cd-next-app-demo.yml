name: cd-next-app-demo

on:
  push:
    branches:
      - main
    paths:
      - apps/nextjs/**
  workflow_dispatch:

env:
  # codebase
  STORE_PATH: ~/.pnpm-store
  # env
  ENVFILE_CONSOLE: apps/nextjs/.env
  # public
  PK_AUTHDOG: pk_eyJlbnZpcm9ubWVudElkIjoiMjVmN2FmYmQtOWNmZS00MTQyLWI1OWYtOGUyNmI2N2IzZTYyIiwiaWRlbnRpdHlIb3N0IjoiaHR0cDovL2xvY2FsaG9zdDozNTAwIn0=
  

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x]

    steps:
      - uses: actions/checkout@v4
        with:
          show-progress: false

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - uses: pnpm/action-setup@v4
        with:
          version: 9.0.0

      - name: Use taobao registry
        run: npm config set registry https://registry.npmmirror.com/

      - name: Setup PNPM store path
        run: pnpm config set store-dir ${{ env.STORE_PATH }}

      - name: Setup Pnpm Cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-cache-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-cache-

      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile

      - name: Prepare console environment variables
        run: |
          {
            # set to vercel
            echo "PK_AUTHDOG=${{ env.PK_AUTHDOG }}"
          } > ${{ env.ENVFILE_CONSOLE }}
        
      - name: Install public SDK
        run: |
          (cd apps/nextjs && pnpm add @authdog/nextjs-app@latest)

      - name: Ship NextJS demo
        run: |
          npx vercel deploy --prod --token ${{ secrets.VERCEL_CD_TOKEN }} --confirm
          ## --env-file ${{ env.ENVFILE_CONSOLE }} --scope authdog --project authdog-nextjs-demo --force